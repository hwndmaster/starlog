<UserControl x:Class="Genius.Starlog.UI.Views.ThreadProfileFilterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:Genius.Starlog.UI.Behaviors"
        mc:Ignorable="d"
        Validation.ErrorTemplate="{x:Null}">
    <Grid>
        <StackPanel Orientation="Vertical" Style="{StaticResource FormPanel}">
            <DockPanel>
                <Label>Threads:</Label>
                <ListBox SelectionMode="Multiple"
                         ItemsSource="{Binding Threads}"
                         MaxHeight="400">
                    <i:Interaction.Behaviors>
                        <behaviors:ListBoxSelectedItemsBehavior SelectedItems="{Binding SelectedThreads}" />
                    </i:Interaction.Behaviors>
                </ListBox>
            </DockPanel>
        </StackPanel>
    </Grid>
</UserControl>
